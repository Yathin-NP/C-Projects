#include <stdio.h>
#include <math.h>
int main()
{
    int num;
    printf("Enter the value\n");
    scanf("%d", &num);

    printf("The absolute value of the number is %d\n",abs(num));
    return 0;
}
